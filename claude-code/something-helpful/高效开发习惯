1. CLAUDE.md
- 对于新项目，使用/init初始化CLAUDE.md，根据实际需求精简和调整生成的内容；
- 在开发时，可以在一个会话结束后调整CLAUDE.md的内容，或者在会话过程中生成对CLAUDE.md调整后的副本，但不要在会话过程中修改CLAUDE.md。

2. 会话、模型、性能选择
- 重要的主体任务使用主模型和高性能来处理，以尽可能减小试错次数；
- 对于与主任务流无关的问题，额外在新会话使用轻量模型和低性能解决；
- 对于需要用到主任务流信息，但又不涉及文件读写且不影响主任务流的小问题，使用/btw直接获得解答；

3. skill
- 在开发过程中，如果多次提出了相似的问题或命令（如debug、撰写文档、调整配置、提交修改等），可以考虑编写为skill；
- skill最好放置在项目根目录下，保持各项目间独立。

4. 上下文节省和聚焦
- 让Claude理解一个系统时，最好使其只读取关键文件或是大文件、多文件中的关键内容；
- 运行相关命令或脚本进行测试时，最好只提取测试时的关键内容输出；
- 将开发工作以阶段化或模块化的方式保存为文档，再基于文档进行实现；
- 在项目根目录创建.claudeignore，排除Claude不需要看的文件。

5. 单次大的开发任务尽量在当天完成，否则上文缓存将消失；如果无法在当天完成，考虑将会话的历史内容精简为一个文档保存下来，下次接着开发时从新会话中读取该文档。

6. 尽可能不要让单次会话的内容达到自动压缩的阈值，如果不可避免，考虑用上面(5.)的策略，或是在一个子任务完成时手动压缩，可以选择使用/compact $(保留xxx内容)。

7. 如果当前任务等待时间较多，且有其他代码不冲突的任务，考虑使用其他编程工具或在cc用其他模型开启其他任务流。
